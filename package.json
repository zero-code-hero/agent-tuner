{
  "name": "agent-tuner",
  "version": "0.1.0",
  "description": "Generate optimized AGENTS.md from codebase analysis",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "bin": {
    "agent-tuner": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "commander": "^12.0.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0"
  }
}

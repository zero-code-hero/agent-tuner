# AGENTS.md

Auto-generated by agent-tuner. Keep this file updated with `agent-tuner .`

## Setup
- Run `npm install` before building or inspecting types, as node_modules (including the bundled .d.ts typings for @earendil-works/pi-coding-agent) are not committed to this checkout.
- Run `npm install` then `npm run build` before use; the @earendil-works/pi-coding-agent SDK (the default backend) must be installed in node_modules for typings and runtime to work.

## Testing
- No dedicated test suite exists. Use the TypeScript build as the validation gate: run `npm run build` (tsc) to confirm changes compile cleanly before committing.
- There is no test or lint script; validate changes by running `npm run build` (which runs `tsc`), since type-checking via the TypeScript build is the de facto test gate for this repo.
- There is no test or lint script in package.json; the de facto validation gate is type-checking via `npm run build` (runs `tsc`). Run it before committing changes.
- No fixture/golden directory or offline deterministic scoring harness exists; the tool relies on live LLM backends. To dry-test the loop without API calls, use `npx tsx src/cli.ts <dir> --dry-run`.

## Conventions
- Use ESM relative imports with explicit `.js` extensions (e.g. `import { DEFAULT_MODEL } from "./constants.js"`) even though source files are `.ts`; this is required by the NodeNext/ESM tsconfig module setting.

## Architecture
- Agent directory resolution and session/auth/model APIs come from the external @earendil-works/pi-coding-agent package (e.g. getAgentDir, createAgentSession, SessionManager); inspect its bundled .d.ts under node_modules after install to confirm default paths and controlling env vars.

## General
- Running agent-tuner on its own repo is a supported self-improvement workflow: `npx tsx src/cli.ts .` regenerates this repo's AGENTS.md (use --dry-run first to avoid API costs).

# AGENTS.md

Auto-generated by agent-tuner. Keep this file updated with `agent-tuner .`

## Setup
- When modifying TypeScript files in `apps/api`, always check `apps/api/tsconfig.json` for path aliases and compiler options before writing imports to ensure correct module resolution.
- Before implementing or testing API endpoints, review `requests.http` in the repository root for documented request patterns, required headers, payload structures, and query parameters.

## Architecture
- In `apps/*-sdk`, verify that the SDK pins the exact API version it depends on. Update this pin when syncing with API changes to prevent compatibility breaks.

## CI/CD
- When modifying SDKs or API versions, review `.github/workflows/publish-*.yml` to understand automated version synchronization and bumping logic before making changes.
